class Juego {

  // -----------------------------------------------------
  // PROPIEDADES (variables)
  // EstadoGeneralDelJuego = menu, jugar, ganar, perder
  // Arreglo de Monedas 
  // Arreglo de Basura 

  // -----------------------------------------------------
  // CONSTRUCTOR (setup del objeto)
  // EstadoGeneralDelJuego = "menu";
  // inicializar los objetos: contador, monedas, basura, tiempo

  // -----------------------------------------------------
  // METODOS (funciones) 
  // actualizar()
  // --contador.actualizar();
  // --monedas.actualizar();
  // --tiempo.actualizar();
  // --contador.actualizar();
  
  // dibujar()
  // --logicaDeEstados
  // --llamar a los metodos dibujar de mis objetos
  // --monedas.dibujar();
  // --basura.dibujar();
  // --tiempo.dibujar();
  // --contador.dibujar();

  // reciclar ()
  // --basura.reciclar();
  // --monedas.reciclar();
  // --contador.reciclar();
  // --tiempo.reciclar();
  
}
