class Tiempo {

  //-------------------------------------
  // PROPIEDADES > variables
  // variables para posición y tamanio
  // variable para los segundos

  // -----------------------------------------------------
  // CONSTRUCTOR > el "setup de la clase"
  // inicializar las propiedades/variables
  // Inicia en el borde de la pantalla (width)
  // Segundos
  // --carga de las imagenes
  // --modificar el tamaño de las imágenes

  // -----------------------------------------------------
  // METODOS > funciones

  // dibujar()
  // --dibuja el rectángulo que va disminuyendo con los segundos hasta llegar a 0 (extremo izq.)
  
  // actualizar()
  // --actualizo los segundos con map 
  
  // reciclar()
  // --vuelve a llenarse la barra
}
